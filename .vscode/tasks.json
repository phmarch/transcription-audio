{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "i: initialize a runner (run from its directory)",
            "type": "shell",
            "command": "cp ../pyproject.toml.template pyproject.toml"
        }
    ]
}